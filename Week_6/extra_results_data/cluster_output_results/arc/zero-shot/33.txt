The provided code is a part of the Lucene library and implements the functionality for encoding and decoding per-document vectors and associated indexing structures required for nearest-neighbor search. The code includes the `KnnVectorsFormat` class, which is an abstract class that provides methods for writing and reading vectors to and from an index. The `Holder` class is a static holder class that prevents classloading deadlock by delaying initialization of doc values formats until needed. The `forName` method looks up a format by name, the `fieldsWriter` method returns a `KnnVectorsWriter` to write the vectors to the index, and the `fieldsReader` method returns a `KnnVectorsReader` to read the vectors from the index. The `EMPTY` format is a sentinel indicating a codec that does not support vectors. The code is part of the Lucene library, which is a high-performance, full-featured text search engine library written entirely in Java. It is widely used in search applications and provides powerful indexing and search capabilities.