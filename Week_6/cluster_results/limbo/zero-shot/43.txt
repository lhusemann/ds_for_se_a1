This code is a reader for plaintext stored fields. It is used for recreational purposes only. The code reads a stored fields file in entirety up-front and saves the offsets so it can seek to the documents later. It also has the functionality to visit a specific document and read its stored fields. The code is written in Java and uses the Lucene library.

Title: Plaintext Stored Fields Reader