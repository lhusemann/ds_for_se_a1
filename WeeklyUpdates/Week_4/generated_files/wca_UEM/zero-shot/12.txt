The provided code is an implementation of the FST {@link Outputs} interface for {@link FSTTermsWriter}. It provides functionality for encoding and decoding term data in the form of {@link TermData} objects. The code includes methods for writing and reading {@link TermData} objects to and from a {@link DataOutput} and {@link DataInput} respectively. It also includes methods for calculating the common and subtracting {@link TermData} objects, as well as methods for calculating the RAM usage of {@link TermData} objects. Additionally, the code includes methods for checking the equality of {@link TermData} objects based on their statistics and byte arrays. The code also includes a nested {@link TermData} class that represents the metadata for one term, including byte arrays, document frequency, and total term frequency.