**Title:** Lucene Header Utilities

**Summary:**
This module provides utility methods for reading and writing versioned headers in Lucene. It includes methods for writing and reading headers, as well as checking the validity of the headers. The core logic of this code is to provide functionality for reading and writing headers in Lucene. It includes methods for writing headers with a specified codec name and version number, as well as reading headers and checking their validity.

The module depends on the Lucene store classes for input and output streams, as well as the StringHelper utility class for working with object IDs and suffixes. The provided code snippet includes methods for writing and reading headers, as well as checking the validity of the headers. It also includes methods for computing the length of headers and retrieving the checksum of a file.

The architecture follows a clear separation of concerns, with the header writing and reading logic encapsulated in the provided utility methods. The code is designed to be used in a Lucene-specific context, and its dependencies are limited to the Lucene API. The code is also thread-safe and ensures the integrity of the written headers.