**Title:** `PointsReader` class for visiting point values
**Summary:** The `PointsReader` class is part of the `org.apache.lucene.codecs` package and implements the `Closeable` interface. It provides an abstract API for visiting point values and includes methods for checking the integrity of the reader and getting point values for a given field. The class allows for getting an optimized instance for merging. The core logic involves implementing the abstract API for visiting point values and providing methods for checking the integrity of the reader and getting point values for a given field. The class has the following inputs/outputs: `checkIntegrity()` does not take any inputs and does not return any outputs, `getValues(String field)` takes a `String` field as input and returns a `PointValues` object, `getMergeInstance()` does not take any inputs and returns a `PointsReader` object. The class has the following dependencies: It imports the `java.io.Closeable` and `java.io.IOException` classes, and imports the `org.apache.lucene.index.PointValues` class.