This code provides an abstract API for consuming normalization values and a concrete implementation that writes the norms into the index in a specific format. The API is a "pull" rather than "push", and the implementation is free to iterate over the values multiple times. It also includes a method for merging fields from the readers in a MergeState. Implementations can override this method for more sophisticated merging.