**Title:** PointValues Flush Sorter — Value Order Manipulation for codecs

**Summary:**
The PointValuesFlushSorter class is a utility for codecs that need to optimize the flushing process of PointValues. It provides methods to retrieve the packed bytes of a value, retrieve the k-th byte of a value, retrieve the doc ID of a value, swap two values, save a value to temporary storage, and restore values from temporary storage.

The class extends the PointValues class and provides additional functionality to manipulate the order of values. This allows codecs to optimize the flushing process by rearranging the values in a way that suits their specific needs.

The PointValuesFlushSorter class is designed to be used by codecs that need to optimize the flushing process of PointValues. It provides methods to retrieve the packed bytes of a value, retrieve the k-th byte of a value, retrieve the doc ID of a value, swap two values, save a value to temporary storage, and restore values from temporary storage.

The class isolates the manipulation of value order from the core PointValues class, allowing codecs to use the flush sorter without modifying the core logic of PointValues. It provides a clean separation of concerns, making it easier for codecs to optimize the flushing process without affecting the core functionality of PointValues.

The PointValuesFlushSorter class is a low-level utility that is specific to the needs of codecs. It is not expected to be used directly by most users of Lucene. Instead, it is intended to be used by codecs that need to optimize the flushing process of PointValues.

The PointValuesFlushSorter class is a stateless utility, meaning that it does not maintain any state between method calls. It is designed to be used by codecs that need to optimize the flushing process of PointValues, and its methods are expected to be called in a specific order.

The PointValuesFlushSorter class is thread-safe, meaning that it can be used concurrently by multiple threads without any race conditions or data corruption. It is designed to be used by codecs that need to optimize the flushing process of PointValues, and its methods are expected to be called in a specific order.

The PointValuesFlushSorter class is designed to be used by codecs that need to optimize the flushing process of PointValues. It provides methods to retrieve the packed bytes of a value, retrieve the k-th byte of a value,