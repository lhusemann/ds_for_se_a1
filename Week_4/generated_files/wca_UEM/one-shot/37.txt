**Title:** SegmentInfoFormat — Controls the Format of SegmentInfo in Lucene

**Summary:**
The SegmentInfoFormat class is responsible for controlling the format of the SegmentInfo (segment metadata file) in the Lucene library. It provides two methods: `read` and `write`. The `read` method reads segment information from a directory, given a segment name and an expected segment identifier. The `write` method writes segment information to a directory, given a SegmentInfo object and an IOContext.

The `read` method populates a SegmentInfo object by reading data from a directory and returns the object. The `write` method serializes the SegmentInfo object and writes it to a directory.

The class has no shared mutable state and is thread-safe. It depends on the Directory interface for reading and writing to a file system, as well as the IOContext class for I/O operation context.

The SegmentInfoFormat class is a low-level utility that isolates the format of the SegmentInfo file from the rest of the Lucene library. It is designed to be used by higher-level components that need to read or write segment metadata.

The code snippet provided is a concise and precise summary of the SegmentInfoFormat class, highlighting its key functionality, core logic, inputs/outputs, and dependencies.