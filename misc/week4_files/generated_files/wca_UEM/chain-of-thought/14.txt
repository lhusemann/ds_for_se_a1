**Title:** Lucene's PostingsConsumer class
**Summary:** The provided code is a Java class that is part of the Lucene library. It is an abstract class that provides an API for consuming terms, doc, freq, prox, offset, and payloads postings. The class is part of the Lucene codecs package and is used for writing postings to an index in a specific format. The class has a constructor and several methods, including write and merge. The write method is used to write all fields, terms, and postings to an index. It takes a Fields instance and a NormsProducer as parameters and is responsible for computing index statistics, skipping terms with no docs, and writing fields and terms lazily. The merge method is used to merge fields from multiple readers and is responsible for mapping around deleted documents. The class also implements the Closeable interface, which allows it to be closed when it is no longer needed. The class is marked as experimental, which means that it is still in development and may change or be removed in future versions of Lucene.